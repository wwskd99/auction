<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.mapper.MemberMapper">

<select id="readMember" resultType="org.zerock.domain.MemberVO">
	select * from Member where user_id = #{user_id};
</select>
<insert id="joinMember">
	insert into Member (user_id, name, pw, phone, birth) values (#{user_id}, #{name}, #{pw}, #{phone}, #{birth})
</insert>
<delete id="resignMember">
	DELETE FROM Member where user_id = #{user_id};
</delete>
<update id="updateMember"> 
	update Member set 
	name = #{name},
	pw = #{pw},
	phone = #{phone},
	birth = #{birth}  
	where user_id = #{user_id} 
</update>
</mapper>